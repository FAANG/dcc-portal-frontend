it('should sort table on column Ontology Id', () => {
  ontologyPage.compare_value('.cdk-column-ontology_id')
});

it('should sort table on column Project', () => {
  ontologyPage.compare_value('.cdk-column-project')
});

it('should sort table on column Project', () => {
  ontologyPage.compare_value('.cdk-column-tags')
});
